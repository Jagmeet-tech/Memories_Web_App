import {makeStyles} from "@material-ui/core/styles";

export default makeStyles(()=>({    //makestyles called and passed an callback fn which returns json object.
    ul:{
        justifyContent:"space-around",
    },
}));